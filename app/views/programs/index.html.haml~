%div{:style => 'margin: 30px'}
  %h1 Programs

  - @programs.each do |program|
    %tbody
      %tr
        %td= link_to "#{program.name}", program_path(program.id)
        %td= program.start_date
        %td= program.end_date
        %td= program.pending

  %br

  = link_to 'Back to Dashboard', dashboard_path
